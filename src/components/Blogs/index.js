import React from 'react'

import { Container } from '@mui/material';

const Blogs = () => {
  return (
    <Container sx={{
      display: 'flex', justifyContent: 'center', p:'100px'
    }}>
      Currently Under Construction (as is the rest of the website)
    </Container>
  )
}

export default Blogs
